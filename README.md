# moex - Python library for simple iss API usage

Some functions for data loading from moex api

## Installation

Run into shell ```git clone 'https://github.com/alexeydtarasov/moex/' && cd moex && python install.py```

